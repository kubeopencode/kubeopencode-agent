# Copyright Contributors to the KubeOpenCode project
# Kustomization for scheduled CronJob resources.
#
# Prerequisites:
# 1. Apply base resources first: kubectl apply -k ../base
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubeopencode-scheduled
resources:
  - namespace.yaml
  - rbac.yaml
  - cronjob-tiny-refactor.yaml
  - cronjob-opencode-update.yaml
  - cronjob-pr-review.yaml
